{
  "name": "reroll-counter",
  "version": "1.0.0",
  "private": true,
  "description": "A simple React app to count rerolls for rare probability events, ready for Netlify.",
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  }
}